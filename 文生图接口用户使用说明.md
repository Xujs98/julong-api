# 文生图接口用户使用说明

这是一份给普通用户和智能体使用的简明文档。智能体拿到本文后，可以根据用户的描述组装请求，调用接口生成图片，并把返回的图片地址或图片数据交给用户。

## 1. 接口信息

- **接口地址**：`https://api.julongkj.top/v1/images/generations`
- **请求方法**：`POST`
- **请求类型**：`application/json`
- **鉴权方式**：`Authorization: Bearer <你的 API Key>`

API Key 示例：

```text
sk-xxxxxxxxxxxxxxxx
```

不要把真实 API Key 写入公开提示词、网页源码、代码仓库或截图。

## 2. 请求参数

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `model` | string | 是 | 图片模型名称，例如 `gpt-image-2`。以账号实际可用模型为准。 |
| `prompt` | string | 是 | 图片生成提示词，描述主体、场景、风格、构图、光线和文字要求。 |
| `size` | string | 否 | 图片尺寸，例如 `1024x1024`、`1024x1792`、`1792x1024`。以模型支持的尺寸为准。 |
| `quality` | string | 否 | 图片质量。常用值为 `auto`、`low`、`medium`、`high`；部分模型使用 `standard` 或 `hd`，以模型支持值为准。 |
| `n` | integer | 否 | 生成数量，默认 `1`，范围为 `1-128`。JSON 中请传数字，不要传字符串。 |
| `response_format` | string | 否 | 返回格式。填写 `b64_json` 时返回 Base64 图片数据；填写 `url` 或省略时通常返回图片地址，具体以模型为准。 |
| `async` | boolean | 否 | 是否使用异步轮询。默认 `false`。填写 `true` 时需要按任务 ID 查询状态，详见《生图轮询接口说明》。 |

建议首次调用只使用 `model`、`prompt`、`n`，确认模型可用后再增加尺寸、质量等参数。

## 3. 最简单的调用

```bash
curl https://api.julongkj.top/v1/images/generations \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer 你的API_KEY" \
  -d '{"model":"gpt-image-2","prompt":"一张极简产品海报","n":1}'
```

带尺寸、质量和 Base64 返回格式的示例：

```bash
curl https://api.julongkj.top/v1/images/generations \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer 你的API_KEY" \
  -d '{
    "model": "gpt-image-2",
    "prompt": "一张极简风格的无线耳机产品海报，白色背景，柔和侧光，产品位于画面中央，顶部留出标题空间，商业摄影质感",
    "size": "1024x1024",
    "quality": "high",
    "n": 1,
    "response_format": "b64_json"
  }'
```

## 4. 返回结果怎么处理

### 4.1 返回图片地址

返回结果通常类似：

```json
{
  "created": 1784422828,
  "data": [
    {
      "url": "https://图片地址.example/image.png"
    }
  ]
}
```

如果 `url` 是完整的 `http://` 或 `https://` 地址，直接打开或下载即可。

如果 `url` 以 `/v1/` 开头，这是矩龙-API 的受保护图片地址。下载时在前面拼接服务地址，并携带同一个 API Key：

```bash
curl -L "https://api.julongkj.top<返回的 url>" \
  -H "Authorization: Bearer 你的API_KEY" \
  -o generated-image.png
```

### 4.2 返回 Base64 图片数据

当 `response_format` 设置为 `b64_json` 时，结果中的 `data[0].b64_json` 是 Base64 字符串。智能体应当：

1. 读取 `data` 数组中的每一项。
2. 取出 `b64_json` 字段。
3. Base64 解码为图片文件。
4. 根据实际图片格式保存为 `.png`、`.jpg` 或模型返回的对应格式。
5. 将图片文件或可访问路径交给用户。

如果 `n` 大于 `1`，请按 `data[0]`、`data[1]`、`data[2]` 的顺序处理每一张图片。

## 5. 给智能体的直接执行规则

将下面的规则交给智能体即可：

```text
当用户要求生成图片时：
1. 从用户描述中提取主体、场景、风格、构图、色彩、光线、画面比例和文字内容。
2. 生成清晰、具体、可执行的中文 prompt；用户指定的文字必须原样放入 prompt 并注明“文字清晰、准确、不可改写”。
3. 默认使用 model=gpt-image-2、n=1、size=1024x1024、quality=auto。
4. 调用 POST https://api.julongkj.top/v1/images/generations，并在 Authorization 头中使用用户提供的 API Key。
5. 优先请求 response_format=url；如果当前工具需要直接取得二进制图片，再请求 response_format=b64_json。
6. 读取返回 JSON 的 data 数组：有 url 就打开或下载，有 b64_json 就解码保存。
7. n 大于 1 时逐张处理并返回所有结果。
8. 出现 error.message 时向用户说明具体原因，不要把 API Key 输出到对话中。
```

智能体请求体模板：

```json
{
  "model": "gpt-image-2",
  "prompt": "主体：{{主体}}；场景：{{场景}}；风格：{{风格}}；构图：{{构图}}；光线与色彩：{{光线与色彩}}；画面比例：{{画面比例}}；文字要求：{{文字要求}}；其他限制：{{其他限制}}",
  "size": "1024x1024",
  "quality": "auto",
  "n": 1,
  "response_format": "url"
}
```

## 6. Prompt 编写模板

推荐按以下顺序描述：

```text
主体 + 动作/状态 + 场景 + 视觉风格 + 构图/镜头 + 光线/色彩 + 文字要求 + 画质要求 + 限制条件
```

示例：

```text
一只橘猫坐在窗边木桌上，午后阳光从右侧照入，室内植物作为背景，温暖治愈的生活方式摄影，半身构图，浅景深，细腻毛发，真实自然的光影，不要水印，不要多余文字。
```

带海报文字的示例：

```text
极简黑白咖啡品牌海报，白色留白背景，黑色咖啡杯位于中央，顶部显示中文“每日一杯好咖啡”，底部显示英文“GOOD COFFEE EVERY DAY”，所有文字清晰、准确、不可改写，现代平面设计，均衡留白，高级印刷质感。
```

## 7. 同步与异步选择

- **同步方式**：省略 `async` 或设置为 `false`。请求完成后直接返回图片结果，适合单次调用。
- **异步方式**：设置 `async: true`。接口先返回 `task_id`，智能体按 `polling_interval_seconds` 查询 `/v1/images/generations/<task_id>`，直到 `status` 变为 `success` 或 `failed`。
- 异步方式依赖管理员开启“记录生图日志”；未开启时会自动按同步方式返回。
- 异步任务的完整提交、轮询、下载和错误处理见：`生图轮询接口说明.md`。

异步提交示例：

```json
{
  "model": "gpt-image-2",
  "prompt": "一张极简产品海报",
  "n": 1,
  "async": true
}
```

## 8. 常见错误

| HTTP 状态码 | 常见原因 | 处理方式 |
| --- | --- | --- |
| `400` | JSON 格式错误、缺少 `model`/`prompt`、尺寸或数量不符合模型要求 | 修正请求参数后重试。 |
| `401` | API Key 缺失、错误或已失效 | 检查 `Authorization: Bearer <API_KEY>`。 |
| `403` | 账号、Token 或来源被限制 | 检查账号状态和可用权限。 |
| `404` | 异步任务不存在，或任务不属于当前 API Key | 使用原 API Key 和正确的 `task_id`。 |
| `503` | 当前模型暂时没有可用渠道 | 更换模型或稍后重试。 |

错误响应通常包含：

```json
{
  "error": {
    "message": "错误原因",
    "type": "invalid_request_error"
  }
}
```

## 9. 使用建议

- 使用具体描述替代“好看一点”等模糊要求。
- 需要图片中的文字时，把文字放在引号中，并要求“清晰、准确、不可改写”。
- 先用 `n: 1` 验证提示词，再增加生成数量。
- 尺寸使用英文小写 `x`，例如 `1024x1024`，不要使用乘号 `×`。
- 保留返回的 `url` 或 `task_id`，便于后续下载和查询。
- 不要在日志、网页、提示词或对话中输出完整 API Key。
